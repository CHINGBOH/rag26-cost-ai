/**
 * RAG 系统内部技术文档知识库
 * 每个 Section 代表一个主题段落，供 SystemAssistant 做关键词检索后注入 prompt。
 *
 * 修改原则：与代码保持同步。修改了架构/工具/配置后，同步更新这里。
 */

export interface DocSection {
  id: string;
  title: string;
  keywords: string[];   // 检索关键词
  content: string;       // 注入 prompt 的正文
}

export const SYSTEM_DOCS: DocSection[] = [
  {
    id: 'architecture-overview',
    title: '系统总体架构',
    keywords: ['架构', '系统', '组成', '服务', '后端', '前端', '微服务', '整体', '模块', '技术栈', '是什么', '做什么'],
    content: `# RAG 智库系统 — 总体架构

RAG 智库是一套多服务 RAG（检索增强生成）系统，俗称"四库"，指它同时运行四种数据库。

## 五个服务进程
| 服务 | 端口 | 语言/框架 | 职责 |
|------|------|---------|------|
| Go 网关 (gateway) | 8080 | Go/Gin | 统一入口，路由所有前端请求 |
| Go WebSocket | 8081 | Go/Gin | 实时推送运行状态到前端 |
| Python 检索服务 | 8002 | FastAPI | Agent 运行、工具调用、RAG 流水线 |
| Python 历史服务 | 8000 | FastAPI | 文档上传、历史记录、嵌入索引 |
| Node.js 协调器 | 3000 | Fastify+XState v5 | LLM 代理、会话管理、状态机 |
| React 前端 | 5173/8888 | React 18 + Vite | 用户界面 |

## 请求完整链路
用户提问 → Vite Proxy → Go 网关(8080) → Python 检索服务(8002) → Agent 决策 → 工具调用 → 四库检索 → LLM 生成答案 → SSE 流式推送 → 前端展示`,
  },

  {
    id: 'four-databases',
    title: '四库数据库架构',
    keywords: ['四库', 'qdrant', 'postgresql', 'neo4j', 'elasticsearch', '向量', '数据库', '索引', '存储', '检索', '全文', '图', '关系'],
    content: `# 四库数据库架构

## Qdrant — 向量库
- 存储：每段文本的 768 维嵌入向量
- 作用：语义相似度检索，理解"意思相近"的内容
- 嵌入模型：BGE-M3 (通过 TEI 服务生成)
- 集合名：knowledge_base
- 适合：模糊语义查询、主题相关内容

## PostgreSQL — 关系库（主数据库）
- 存储：文档元数据、价格记录、用户会话、运行日志
- 核心表：documents, chunks, price_records, sessions, agent_runs
- 适合：精确数字查询、结构化过滤、价格/费率数据
- 支持 zhparser 中文分词全文检索

## Neo4j — 知识图谱
- 存储：概念节点 + 关系边（如"企业管理费" → 属于 → "费率标准"）
- 作用：概念关联推理、路径检索、语义扩展
- 适合：问"A 和 B 有什么关系？"、拓扑查询

## Elasticsearch — 全文搜索
- 存储：文本块的分词倒排索引
- 作用：中文关键词精确匹配、术语查找
- 适合：精确术语匹配、布尔组合查询

## 四库协同
hybrid_search 工具会同时查询多个库，然后用 RRF（互惠排名融合）算法合并结果，取最相关的 top-K 段落。`,
  },

  {
    id: 'tools-search',
    title: '检索类工具',
    keywords: ['工具', '检索', '搜索', 'search', '向量搜索', '关键词', '混合', '全文', '图搜索', 'hybrid', 'vector', 'keyword', 'text', 'pdf', '条文'],
    content: `# 检索类工具（Agent 自动选择）

## hybrid_search — 混合检索（主力工具）
- 同时查 Qdrant 向量 + PostgreSQL trgm + Elasticsearch 全文
- RRF 算法融合排名
- 参数：query, top_k(默认10), path_constraint(路径约束)
- 适用：大多数问题的第一选择

## vector_search — 纯向量检索
- 仅查 Qdrant 向量相似度
- 适用：语义模糊表达，如"大概讲了什么"

## keyword_search — 关键词检索
- 仅查 Elasticsearch 分词匹配
- 适用：需要精确术语、专业词汇

## concept_search — 知识图谱概念检索
- 在 Neo4j 中查找与关键词关联的概念节点
- 适用：概念扩展、找关联知识

## graph_search / topology_search — 图路径检索
- 沿知识图谱边推理
- topology_search 支持 max_depth 参数（最多查几跳关系）

## text_search — 中文分词全文检索
- PostgreSQL tsquery 语法
- 支持 zhparser（若已安装）

## pdf_page_search — PDF 原页检索
- 保留 PDF 原始版面信息检索
- 返回页码信息，适合"在第几页"类问题

## rule_clause_search — 条文条款专项检索
- 专门针对规范/标准中的条款结构检索
- 返回条款编号、层级结构`,
  },

  {
    id: 'tools-price',
    title: '价格查询工具',
    keywords: ['价格', '信息价', '定额', '费率', '材料', '规格', '月份', '趋势', 'price', 'price_query', 'price_trend', '造价', '含税'],
    content: `# 价格查询工具

## price_query — 价格精确查询
- 参数：material_name（材料名）, specification（规格型号）, year_month（年月如"2026-01"）, top_k
- 数据来源：PostgreSQL price_records 表
- 返回：含税价、不含税价、规格、来源月份
- 示例：material_name="普通硅酸盐水泥", specification="P.O42.5", year_month="2026-01"
- 若不指定 year_month，自动取最新月份

## price_trend — 价格趋势分析
- 参数：material_name, start_month, end_month
- 返回：按时间序列的价格变化数据
- 前端会自动渲染成折线图（沙盘展示区）
- 适用："过去一年水泥价格走势"类问题

## 价格查询注意事项
- 数据仅限已索引的信息价文档范围
- 材料名需与数据库中一致（系统会自动做模糊匹配扩展）
- 若查不到返回"无匹配记录"，不会猜测`,
  },

  {
    id: 'tools-compute',
    title: '计算工具（沙盒）',
    keywords: ['计算', '沙盒', '公式', 'calculator', 'python_eval', '数学', '执行', '代码', '步骤', '结果', '安全', '隔离'],
    content: `# 计算工具（安全沙盒）

## calculator — 数学表达式计算器
- 接受标准数学表达式：500 * 0.08, sqrt(16), round(3.14159, 2)
- 基于 Python math 模块，在隔离进程中执行
- 安全限制：无文件 IO、无网络、无 import、执行超时 2 秒
- 返回：计算结果 + 中间步骤说明

## python_eval — Python 代码沙盒
- 接受完整 Python 代码片段
- chunks_json 参数：把检索到的数据传给代码使用
- 支持 numpy、math、statistics 等安全库
- 禁止：os/sys/subprocess/open 等危险模块
- 适用：复杂多步计算、统计分析、批量处理

## 计算安全保障
- 独立子进程，资源限制（CPU/内存）
- 超时自动终止
- 异常完整捕获并返回友好错误信息

## 计算过程展示
计算完成后，前端"运行沙盘"面板会展示：
- 完整计算步骤
- 公式展示
- 最终结果
- 单位说明`,
  },

  {
    id: 'tools-database',
    title: '数据库查询工具',
    keywords: ['sql', 'sql_query', 'aggregate', '表', '字段', 'list_tables', 'describe_table', '数据库', '统计', '查询', '结构化'],
    content: `# 数据库直接查询工具

## list_tables — 列出所有数据表
- 返回 PostgreSQL 中的表名列表
- 帮助 Agent 了解有哪些可查的数据

## describe_table — 查看表结构
- 参数：table（表名）
- 返回：列名、数据类型、约束

## sql_query — 执行 SQL 查询
- 参数：sql（SELECT 语句）, max_rows（最多返回行数，默认50）
- 只读权限：仅允许 SELECT，禁止 INSERT/UPDATE/DELETE
- 返回：JSON 格式结果集

## aggregate_query — 聚合统计查询
- 专门用于 GROUP BY / COUNT / SUM / AVG 等统计操作
- 针对大数据量优化

## 数据安全
- 所有查询在只读事务中执行
- 超时自动回滚
- 结果限制行数防止爆内存`,
  },

  {
    id: 'retrieval-pipeline',
    title: '完整检索流水线',
    keywords: ['流水线', '流程', '步骤', '怎么工作', '如何检索', '查询改写', 'rerank', '重排', 'rrf', '融合', '召回', '路由', '分析'],
    content: `# 完整检索流水线（每次提问的内部过程）

## 第一步：查询分析与路由
- 判断问题类型：价格查询 / 费率标准 / 条文规范 / 计算问题 / 综合查询
- 识别关键实体：材料名、规格、时间、计算参数
- 决定路由：直接查价格表 vs 文档语义检索 vs 需要计算

## 第二步：查询改写
- 扩展同义词和专业术语变体
- 生成多个查询变体提高召回率
- 例："水泥价格" → ["普通硅酸盐水泥信息价", "P.O42.5 含税价格", ...]

## 第三步：多路并行检索
- hybrid_search 同时命中 Qdrant + PostgreSQL + Elasticsearch
- 配置 top_k=8（系统默认，可调整）
- 每路返回各自最相关的候选段落

## 第四步：RRF 融合排名
- 互惠排名融合（Reciprocal Rank Fusion）
- 多路结果加权合并，不偏袒任何单一检索方式
- 产出全局最优排名

## 第五步：重排序（Rerank）
- 精排模型对 top-K 候选二次打分
- 阈值：0.60（低于此分数的段落不会被采用）
- 过滤掉低相关度内容

## 第六步：LLM 生成
- 将精排后的段落作为上下文
- 使用 deepseek-v4-flash 生成最终答案
- 严格要求：基于检索内容作答，不猜测无依据信息

## 第七步：置信度评估
- 从四个维度评估：完整性 / 一致性 / 事实一致性 / 来源多样性
- 综合置信度 < 0.60：返回"当前索引信息不足以回答此问题"
- 来源溯源：记录每段内容的 doc_id、页码、数据库来源`,
  },

  {
    id: 'anti-hallucination',
    title: '防幻觉机制',
    keywords: ['幻觉', '幻想', '不确定', '信息不足', '置信度', '可靠', '准确', '拒绝', '为什么不回答', '没有结果', '告知'],
    content: `# 防幻觉机制

## 什么是幻觉？
LLM"幻觉"是指模型在没有事实依据的情况下生成听起来合理但实际上错误的内容。

## 本系统的防幻觉策略

### 1. 检索优先（RAG 基础）
所有答案必须基于检索到的真实文档内容。LLM 不依靠自身"记忆"作答。

### 2. 置信度阈值（0.60）
每次检索后，系统从四个维度评分：
- 完整性：检索到的内容是否覆盖了问题的关键点
- 一致性：多段检索内容之间是否互相印证
- 事实一致性：生成的答案是否与检索内容吻合
- 来源多样性：是否有多份文档佐证

综合分 < 0.60 → 系统明确告知"信息不足"，不强行给出答案。

### 3. 来源溯源
每个答案标注来源文档 ID、章节、页码。用户可点击查看原文。

### 4. 拒绝猜测
系统指令明确禁止 LLM 猜测或补全缺失信息。
遇到超出知识库范围的问题，直接说明而非捏造。

## 为什么说"信息不足"？
常见原因：
- 相关文档尚未上传到知识库
- 上传的文档尚未完成索引（进度在系统运维页可查）
- 问法太模糊，无法召回精确内容
- 问的是系统本身没有录入的外部信息

## 如何解决？
1. 换角度提问（更具体：加规格型号、年月等）
2. 到"文档管理"页面确认目标文档已上传并索引完成
3. 如果文档确实没有，需要先上传`,
  },

  {
    id: 'indexing-mechanism',
    title: '文档索引机制',
    keywords: ['索引', '上传', '文档', '处理', 'pdf', '分块', 'chunk', '嵌入', '向量化', '入库', '流程', '怎么加文档'],
    content: `# 文档索引机制（如何让文档进入知识库）

## 完整索引流程
1. **上传**：通过"文档管理"页面上传 PDF / Word / TXT 文件
2. **解析**：OCR 服务（PaddleOCR）提取文本，保留页码信息
3. **分块**：按语义分割成 300-500 字的 Chunk（文本块）
4. **嵌入向量化**：BGE-M3 模型将每个 Chunk 转成 768 维向量
5. **写入四库**：
   - Qdrant：存向量
   - PostgreSQL：存 Chunk 文本 + 元数据
   - Elasticsearch：建分词倒排索引
   - Neo4j：提取概念节点和关系（可选）
6. **完成**：文档状态变为"已索引"，可参与检索

## 索引进度查看
"系统运维"页面 → 服务状态 → 可查看：
- 已索引文档数量
- 文本块总数
- 各库状态（正常/降级）

## 常见问题
- PDF 扫描件：需要 OCR，时间较长（取决于页数）
- 表格识别：自动提取为结构化数据（若格式规整）
- 同名文档：系统按 doc_id 去重，重复上传会更新而非创建副本

## 嵌入模型
- 模型：BGE-M3（百川开源多语言向量模型）
- 向量维度：768
- 服务：TEI（Text Embeddings Inference），独立微服务`,
  },

  {
    id: 'usage-guide',
    title: '使用指南与提问技巧',
    keywords: ['怎么用', '使用', '提问', '技巧', '方法', '建议', '有效', '最佳', '推荐', '如何', '帮助', '入门', '新手'],
    content: `# 使用指南与提问技巧

## 功能模块导览
| 页面 | 功能 | 适合场景 |
|------|------|---------|
| 企业知识库（首页）| 极简问答，对话馆员 | 日常业务查询，快速得到答案 |
| RAG 问答（专业版）| 3 列布局，可调参数，看推理过程 | 深入分析、需要看检索来源 |
| 系统运维 | 服务状态、文档数量 | 管理员确认系统健康 |
| 数据管道 | 文档上传索引进度 | 上传新文档后确认索引状态 |
| 学习分析 | 查询质量统计 | 优化提问方式 |

## 价格查询最佳实践
✅ 好的提问：**"2026年1月深圳地区 P.O42.5 普通硅酸盐水泥含税价"**
❌ 模糊提问："水泥多少钱"（缺少规格和时间）

## 费率/定额查询最佳实践
✅ 好的提问：**"建筑工程一般计税下，企业管理费的费率是多少？"**
❌ 模糊提问："管理费怎么算"（缺少工程类别和计税方式）

## 计算问题最佳实践
✅ 好的提问：**"某工程人工费 500 万元，按一般计税方式，企业管理费是多少？"**
系统会：1）查费率 2）自动计算 3）展示步骤

## 提问方法论
1. 给定：**对象**（什么材料/费用/工程）
2. 给定：**限定**（规格型号 / 时间段 / 计税方式）
3. 给定：**目标**（查价格 / 查费率 / 计算结果 / 解释含义）

## 系统局限
- 只能回答知识库中有的内容
- 不能联网查询实时信息
- 不能修改数据库中的记录
- 计算仅限数值运算，不包含工程量测量`,
  },

  {
    id: 'agent-runtime',
    title: 'Agent 运行机制',
    keywords: ['agent', '智能体', '运行', '迭代', '循环', 'xstate', '状态机', '节点', '轨迹', '步骤', '决策', '推理'],
    content: `# Agent 运行机制

## 整体设计
RAG 系统的核心是一个**多步骤 Agent**，它不是一次性生成答案，而是：
1. 分析问题
2. 选择工具
3. 执行工具
4. 观察结果
5. 决定继续还是回答
6. 重复直到能给出完整答案

这是一种"思考→行动→观察→再思考"的 ReAct 范式。

## XState v5 状态机（Node.js 协调器）
- 前端通过 Node.js 协调器的 XState 状态机管理会话
- 状态包括：idle → querying → thinking → tool_calling → answering → done
- 每个状态转换通过 WebSocket 实时推送到前端

## 最大迭代次数
- 默认：3 次（图书馆模式）
- 专业版：用户可调整
- 每次迭代 = 一次工具调用 + 一次 LLM 推理

## Agent 运行页（RAG 问答专业版右侧面板）
实时展示：
- 共享状态（Channel）：当前已获取的数据
- 规划步骤（Plan）：Agent 下一步打算做什么
- 工具调用（Tool Calls）：调用了什么工具、传了什么参数、拿到什么结果
- 节点轨迹（Node Trajectory）：经过了哪些推理节点
- 历史记录（Server Traces）：完整服务端运行日志`,
  },

  {
    id: 'system-config',
    title: '系统配置参数',
    keywords: ['配置', '参数', 'top_k', '阈值', '模式', 'hybrid', 'dense', 'bm25', '分数', '设置', 'score_threshold', 'max_iterations', '温度'],
    content: `# 系统配置参数（专业版可调）

## 检索参数
| 参数 | 默认值 | 说明 |
|------|-------|------|
| top_k | 8 | 每次检索返回的候选段落数 |
| score_threshold | 0.60 | 最低相关度阈值，低于此值不采用 |
| search_mode | hybrid | 检索模式：hybrid / dense / keyword |
| max_iterations | 3 | Agent 最大推理迭代次数 |

## LLM 参数
| 参数 | 图书馆模式 | 专业版默认 |
|------|----------|----------|
| model | deepseek-v4-flash | deepseek-v4-flash |
| llm_route | deepseek / auto / local | deepseek |
| temperature | 0.5（助手）/ 0.7（生成）| 可调 |

## 检索模式说明
- **hybrid**：向量 + 全文混合，综合效果最好（推荐）
- **dense**：仅语义向量，适合语义模糊查询
- **keyword**：仅关键词，适合精确术语

## 图书馆模式固定配置
图书馆模式（首页极简界面）对用户隐藏所有参数，固定为：
- top_k=8, score_threshold=0.6, searchMode=hybrid, maxIterations=3
- 这些是经过测试的最优默认值`,
  },

  {
    id: 'faq',
    title: '常见问题 FAQ',
    keywords: ['faq', '问题', '为什么', '怎么', '错误', '没有', '不对', '失败', '慢', '无法', '原因', '解决'],
    content: `# 常见问题 FAQ

## Q: 为什么回答说"当前信息不足"？
A: 系统找到的相关内容得分低于 0.60 阈值。原因：①目标文档未上传 ②文档未完成索引 ③提问太模糊。
解决：换具体提法、检查文档管理页面。

## Q: 为什么有时候回答很慢？
A: Agent 进行了多轮工具调用（最多3次），每次都包含 LLM 推理。复杂问题正常需要 5-15 秒。

## Q: 计算结果可靠吗？
A: 计算在隔离沙盒中执行，数值计算准确。但前提是价格/费率数据已正确索引。

## Q: 能问系统外部的问题吗？
A: 不能。系统只基于已上传的企业文档作答，不联网，不使用 LLM 自身的"通识知识"。

## Q: 如何上传新文档？
A: 顶部导航 → "数据管道" → 上传文件 → 等待索引完成（状态显示"已索引"）

## Q: 文档上传后多久可以查询？
A: 通常 1-5 分钟，取决于文档大小。PDF 扫描件需要 OCR，时间较长。

## Q: 价格数据如何更新？
A: 上传新的信息价文档后，系统自动解析并更新 price_records 表。旧记录保留，按月份区分。

## Q: 系统支持多少种文件格式？
A: 当前支持：PDF（含扫描件OCR）、Word（.docx）、纯文本（.txt）、Excel（.xlsx，结构化数据）

## Q: 回答里的"来源"是什么意思？
A: 每段检索到的内容都对应一份实际文档的某一页。"查阅了X份资料"表示此次检索用了多少段原文作为依据。`,
  },
];
