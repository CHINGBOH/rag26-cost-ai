global:
  scrape_interval: 15s
  evaluation_interval: 15s

# We don't actively scrape — Tempo metrics-generator pushes via remote-write.
# But include a tempo self-scrape for sanity.
scrape_configs:
  - job_name: tempo
    static_configs:
      - targets: ["tempo:3200"]
